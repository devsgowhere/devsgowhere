---

import "../styles/fontawesome/scss/fontawesome.scss";
import "../styles/fontawesome/scss/regular.scss";
import "../styles/fontawesome/scss/solid.scss";

---

<!-- Google Fonts -->
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Audiowide&family=Space+Grotesk:wght@300..700&display=swap" rel="stylesheet">

<!-- Global styles -->
<style lang="scss" is:global>

	// 
	//
	// Reset styles
	//
	//
	* {
		margin: 0;
		padding: 0;
		box-sizing: border-box;
	}
	*::before,
	*::after {
		box-sizing: border-box;
	}

	//
	//
	// Colors
	//
	//

	:root {
		--base-color: #eeeeee;
	}

	//
	// Define grays from gray-1 to gray-8
	//

	$grays: (
		"1": #ededed,
		"2": #dfdfdf,
		"3": #bfbfbf,
		"4": #a0a0a0,
		"5": #808080,
		"6": #606060,
		"7": #404040,
		"8": #202020,
	);

	:root {
		@each $key, $value in $grays {
			--gray-#{$key}: #{$value};
		}
	}

	html {
		background-color: var(--base-color);
	}

	//
	//
	// Typography
	//
	//
	:root {

		// body
		--font-family-body: "Space Grotesk", Arial, sans-serif;

		// headers
		--font-family-header: "Audiowide", Arial, sans-serif;

		// code
		--font-family-code: "Fira Code", monospace;
	}

	html {
		font-family: var(--font-family-body);
	}

	h1, h2, h3, h4, h5, h6 {
		font-family: var(--font-family-header);
	}

	.text-header {
		font-family: var(--font-family-header) !important;
	}

	h1, h2, h3, h4 {
		font-weight: 400;
	}

	code {
		font-family: var(--font-family-code);
	}

	.text-code {
		font-family: var(--font-family-code) !important;
	}

	// text colors from text-gray-1 to text-gray-7
	@each $key, $value in $grays {
		.text-gray-#{$key} {
			color: var(--gray-#{$key}) !important;
		}
	}

	// text sizes from text-2xs to text-3xl
	$text-sizes: (
		"2xs": 0.5rem,
		"xs": 0.75rem,
		"s": 0.875rem,
		"base": 1rem,
		"l": 1.125rem,
		"xl": 1.25rem,
		"2xl": 1.5rem,
		"3xl": 1.875rem,
	);

	@each $size, $value in $text-sizes {
		.text-#{$size} {
			font-size: $value !important;
		}
	}

	.text-bold {
		font-weight: 700 !important;
	}

	.text-italic {
		font-style: italic !important;
	}

	.text-underline {
		text-decoration: underline !important;
	}

	//
	//
	// Margins and paddings
	//
	//

	// Define spaces from 3xs to 4xl
	$spaces: (
		"0": 0,
		"3xs": 0.125rem,
		"2xs": 0.25rem,
		"xs": 0.5rem,
		"s": 0.75rem,
		"base": 1rem,
		"m": 1rem,
		"l": 1.5rem,
		"xl": 2rem,
		"2xl": 4rem,
		"3xl": 8rem,
		"4xl": 16rem
	);

	@each $space, $value in $spaces {

		.m-#{$space} {
			margin: $value !important;
		}
		.mt-#{$space} {
			margin-top: $value !important;
		}
		.mb-#{$space} {
			margin-bottom: $value !important;
		}
		.ml-#{$space} {
			margin-left: $value !important;
		}
		.mr-#{$space} {
			margin-right: $value !important;
		}
		.my-#{$space} {
			margin-top: $value !important;
			margin-bottom: $value !important;
		}
		.mx-#{$space} {
			margin-left: $value !important;
			margin-right: $value !important;
		}

		.p-#{$space} {
			padding: $value !important;
		}
		.pt-#{$space} {
			padding-top: $value !important;
		}
		.pb-#{$space} {
			padding-bottom: $value !important;
		}
		.pl-#{$space} {
			padding-left: $value !important;
		}
		.pr-#{$space} {
			padding-right: $value !important;
		}
		.py-#{$space} {
			padding-top: $value !important;
			padding-bottom: $value !important;
		}
		.px-#{$space} {
			padding-left: $value !important;
			padding-right: $value !important;
		}
	}

	//
	// 
	// Layout
	//
	//

	.container {
		max-width: 1200px;
		margin: 0 auto;
	}

	//
	// 
	// Tags
	//
	//

	.tag-list{

		list-style-type: none;
		display: inline-flex;
		gap: .25em;
		flex-wrap: wrap;
		padding: 0;

	}

	.tag {

		display: inline-block;
		
		background-color: rgb(222, 241, 254);
		color: rgb(0, 128, 255);

		border-radius: 2em;
		padding: .125em .5em;
		font-size: .875em;

	}

</style>
<!-- End: Global styles -->